```shell
# Setup username for global
# 1. git config --global user.name <username>
$ git config --global user.name "tomatkungen"

# 2. Confirm
$ git config --global user.name 

# Setup username for local
# 1. git config user.name <username>
$ git config user.name "tomatkungen"

# 2. Confirm
$ git config user.name
```

```shell
# Setup username for global
# 1.git config --global user.email <email>    
$ git config --global user.email "mr_a2@hotmail.com"

# 2. Confirm
$ git config --global user.email

# Setup username for local
# 1. git config user.email <username>
$ git config user.email "mr_a2@hotmail.com"

# 2. Confirm
$ git config user.email
```