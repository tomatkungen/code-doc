```shell
# Install rust
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh

# installed at /Users/kimkarlsson/.rustup

# Add ios target with rustup
rustup target add aarch64-apple-ios x86_64-apple-ios aarch64-apple-ios-sim
```
