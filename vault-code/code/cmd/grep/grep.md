```shell
# Search in recursive in file structure
$ grep -r <search>

$ grep -f "node-git-json"
```

```shell
# Search recursive in file structure but only with this files
$ grep --include=\*.json -r "nodejs-git-json"
```